INCLUDE (boost_test)
SET (TARGET_NAME smart_vector_test)

ADD_EXECUTABLE (${TARGET_NAME}
    main.cpp
)

TARGET_LINK_LIBRARIES (${TARGET_NAME} ${Boost_UNIT_TEST_FRAMEWORK_LIBRARY})

ADD_UNIT_TEST (${TARGET_NAME})
